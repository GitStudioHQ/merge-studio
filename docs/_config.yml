title: Merge Studio
description: >-
  The merge editor for VS Code and Cursor — a real three-pane merge editor with
  color ribbons, undo history and a conflicts dashboard, plus a side-by-side diff.
theme: jekyll-theme-primer
show_downloads: false
